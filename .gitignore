node_modules/
.radio_playlist/
data/
*.log
*.bak
*.bak.*
.DS_Store
/tmp/
server.pid
worker.pid
# runtime data — regenerate on first run
server.pid
worker.pid
config/device_readings.json

# User-set weather location — runtime config, default is loaded in code
config/weather.json

# Central runtime settings — contains API keys, per-host
config/settings.json
